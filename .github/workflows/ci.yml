name: CI

on:
  push:
    branches: [main, develop]
  pull_request:

permissions:
  contents: read

jobs:
  secret-scan:
    uses: ./.github/workflows/secret-scan.yml
    permissions:
      contents: read

  ci:
    needs: secret-scan
    uses: ./.github/workflows/ci-flutter.yml
